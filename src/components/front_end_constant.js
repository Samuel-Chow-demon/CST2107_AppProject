const CONST_LOG_IN_DELAY_MS = 550;

const CONST_PATH = {
    landing : '/',
    login : '/login',
    signup : '/signup',
    home : '/home'
}

// Current not yet deployed, set as '' first
const DEPLOYED_HOME_URL = '';

export {CONST_PATH, CONST_LOG_IN_DELAY_MS, DEPLOYED_HOME_URL};